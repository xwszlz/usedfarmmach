-- ============================================================================
-- P0 止血（2026-07-24）：将外部合作方「石家庄铁家伙」降级为 admin
-- ============================================================================
-- 依据：《用户体系完善方案_2026-07-24》§3.2 多 super_admin 治理建议（推荐项）
-- 风险：super_admin 与 admin 此前功能无差异；外部合作方持最高权限且无差异 =
--       重大安全风险（无审计抓手、权限过度授予）。
-- 处置：在真实支付接入 / 超管专属能力落地前，先降级收口。
-- 可逆性：如需恢复 super_admin，将下方 'admin' 改回 'super_admin' 重跑即可。
-- 注意：执行前请先确认该邮箱确为外部合作方且无内部明确授权；建议事务包裹 / 先备份。
-- ----------------------------------------------------------------------------

BEGIN;

UPDATE "User"
SET "role" = 'admin',
    "updatedAt" = now()
WHERE "email" = '932133255@qq.com'
  AND "role" = 'super_admin';

-- 校验：受影响的行数应为 1。
--   若返回 0，说明邮箱不匹配或该账号已非 super_admin，请复核后重试。

COMMIT;
